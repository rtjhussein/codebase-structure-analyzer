# Codebase Structure Analyzer

A Python CLI tool that allows users to:

- Select files or folders from a project
- Recursively collect source files
- Export the code to a single file
- Automatically generate a tree representation of the project structure

## Example Output
├── backend

│   ├── controllers

│   └── routes

└── utils
